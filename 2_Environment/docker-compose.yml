version: '3.8'

services:
  # Prometheus server
  prometheus:
    image: prom/prometheus:v2.45.0
    container_name: prometheus
    ports:
      - "9090:9090"
    volumes:
      - ./config/prometheus.yml:/etc/prometheus/prometheus.yml
      - prometheus-data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--storage.tsdb.retention.time=30d'
      - '--storage.tsdb.retention.size=10GB'
    networks:
      - monitoring
    restart: unless-stopped

  # Grafana dashboard
  grafana:
    image: grafana/grafana:10.0.0
    container_name: grafana
    ports:
      - "3000:3000"
    volumes:
      - grafana-data:/var/lib/grafana
      - ./dashboards:/etc/grafana/provisioning/dashboards
    environment:
      - GF_SECURITY_ADMIN_USER=admin
      - GF_SECURITY_ADMIN_PASSWORD=admin
      - GF_INSTALL_PLUGINS=
    networks:
      - monitoring
    restart: unless-stopped
    depends_on:
      - prometheus

  # DaVinci Resolve Exporter (requires custom build)
  # Uncomment if you want to run the exporter in Docker
  # davinci-exporter:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile
  #   container_name: davinci-resolve-exporter
  #   ports:
  #     - "9183:9183"
  #   volumes:
  #     - /c/ProgramData/Blackmagic Design/DaVinci Resolve:/data:ro
  #   environment:
  #     - EXPORTER_PORT=9183
  #     - SCRAPE_INTERVAL=30
  #   networks:
  #     - monitoring
  #   restart: unless-stopped

volumes:
  prometheus-data:
  grafana-data:

networks:
  monitoring:
    driver: bridge
